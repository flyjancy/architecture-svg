interface:
  display_name: "Architecture SVG"
  short_description: "Create Calibri engineering architecture SVGs"
  default_prompt: "Use $architecture-svg to create a Calibri-based engineering architecture diagram with a gray canvas, pale blue/green/peach modules, and black connectors from structured JSON or YAML."
